export default {
    // 更新状态
    priv (state , data) {
        state.priv = data;
    } ,

    route (state , data) {
        state.route = data;
    } ,

    menu (state , data) {
        state.menu = data;
    } ,

    user (state , data) {
        state.user = data;
    } ,

    os (state , data) {
        state.os = data;
    } ,
};